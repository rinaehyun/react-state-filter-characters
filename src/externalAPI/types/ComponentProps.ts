export type CharacterInfoProps = {
    name: string,
    status: string,
    species: string,
    gender: string,
}

export type CharacterProps = {
    searchQuery: string
}

export type PhotoProps = {
    image: string
}